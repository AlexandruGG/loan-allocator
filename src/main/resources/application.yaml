spring:
  h2.console.enabled: true
  datasource:
    url: jdbc:h2:file:./data/la
    username: la_user
    password: la_pass
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
